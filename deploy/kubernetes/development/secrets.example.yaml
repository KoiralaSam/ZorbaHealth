# Copy this file to secrets.yaml and replace placeholder values.
# secrets.yaml is gitignored — never commit real secrets.
#
#   cp secrets.example.yaml secrets.yaml
#   # Edit secrets.yaml with your values, then run Tilt / kubectl apply
#
# Use the same POSTGRES_PASSWORD in postgres-secret and in app-secrets DATABASE_URL.
---
apiVersion: v1
kind: Secret
metadata:
  name: postgres-secret
type: Opaque
stringData:
  POSTGRES_USER: "healthai"
  POSTGRES_PASSWORD: "REPLACE_POSTGRES_PASSWORD"
  POSTGRES_DB: "healthai"

---
apiVersion: v1
kind: Secret
metadata:
  name: rabbitmq-credentials
type: Opaque
stringData:
  username: "guest"
  password: "guest"
  uri: "amqp://guest:guest@rabbitmq:5672/"

---
apiVersion: v1
kind: Secret
metadata:
  name: mailtrap-secrets
type: Opaque
stringData:
  MAILTRAP_API_TOKEN: "REPLACE_WITH_MAILTRAP_API_TOKEN"
  MAILTRAP_FROM_EMAIL: "smartfind@saugatadhikari.tech"
  MAILTRAP_FROM_NAME: "ZorbaHealth"
  # Optional. Mirrors every outbound email to a test inbox for verification.
  MAILTRAP_MIRROR_RECIPIENT: "5wnpn.test@inbox.testmail.app"
  # Optional. Leave unset to use https://send.api.mailtrap.io/api/send.
  # MAILTRAP_SEND_URL: "https://send.api.mailtrap.io/api/send"

---
apiVersion: v1
kind: Secret
metadata:
  name: voipms-credentials
type: Opaque
stringData:
  # Used by notification-service for inbound SMS webhooks + outbound SMS (VoIP.ms REST API).
  # These keys are referenced by deploy/kubernetes/development/notification-service-deployment.yaml.
  username: "REPLACE_VOIPMS_USERNAME"
  password: "REPLACE_VOIPMS_PASSWORD"
  did: "REPLACE_VOIPMS_DID_E164_OR_NANP"
  key: "REPLACE_VOIPMS_API_KEY"   # Must match ?api_key= on the SMS URL in VoIP.ms (generate a long random string)
  voipms_baseurl: "https://voip.ms/api/v1/rest.php"
  # Per SMS-enabled DID in VoIP.ms: Manage DIDs → Edit → SMS/MMS → SMS URL, e.g.
  # https://YOUR_PUBLIC_HOST/sms?api_key=REPLACE_VOIPMS_API_KEY
  # (VoIP.ms calls this with GET query params: from/contact, did, message, …)
  # For outbound sendSMS IP allowlisting, use your public egress IP (or provider docs).
  # HTTP listen addr for notification-service webhook server (matches containerPort in dev k8s).
  http_addr: ":3001"

---
apiVersion: v1
kind: Secret
metadata:
  name: elevenlabs-credentials
type: Opaque
stringData:
  apiKey: "REPLACE_WITH_ELEVENLABS_API_KEY"
  voiceId: "REPLACE_WITH_ELEVENLABS_VOICE_ID"

---
apiVersion: v1
kind: Secret
metadata:
  name: deepgram-credentials
type: Opaque
stringData:
  apiKey: "REPLACE_WITH_DEEPGRAM_API_KEY"

---
apiVersion: v1
kind: Secret
metadata:
  name: openai-credentials
type: Opaque
stringData:
  apiKey: "REPLACE_WITH_OPENAI_API_KEY"

---
apiVersion: v1
kind: Secret
metadata:
  name: livekit-credentials
type: Opaque
stringData:
  apiKey: "REPLACE_WITH_LIVEKIT_API_KEY"
  apiSecret: "REPLACE_WITH_LIVEKIT_API_SECRET"

---
apiVersion: v1
kind: Secret
metadata:
  name: app-secrets
type: Opaque
stringData:
  DATABASE_URL: "postgres://healthai:REPLACE_POSTGRES_PASSWORD@postgres:5432/healthai?sslmode=disable"
  AUTH_SERVICE_JWT_SECRET: "REPLACE_WITH_STRONG_JWT_SECRET"
  PATIENT_SERVICE_JWT_SECRET: "REPLACE_WITH_STRONG_JWT_SECRET"
  INTERNAL_SERVICE_SECRET: "REPLACE_WITH_INTERNAL_SERVICE_SECRET"
  REDIS_PASSWORD: ""
  # Must include one "%s" placeholder for caller IP
  IP_GEOLOCATION_ENDPOINT_TEMPLATE: "https://REPLACE_GEO_PROVIDER_ENDPOINT/%s"
