# Migration Scripts

This directory is reserved for migration-related helper scripts.

The current migration source of truth is:

- `migrations`
- `Makefile`
- `Tiltfile` local migration resource

Future phases can add helper wrappers here for:

- validation
- repeatable local bootstrap
- CI migration checks
