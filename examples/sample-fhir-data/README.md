# Sample FHIR Data

This directory is reserved for open-source sample FHIR resources and synthetic patient bundles.

Phase 1 creates the directory so contributors can locate it consistently. Later phases are expected to add:

- synthetic patient bundles
- example `Patient`, `Observation`, `Condition`, and related resources
- seed scripts that load the data into the health-records pipeline
