// Code generated by sqlc. DO NOT EDIT.
// versions:
//   sqlc v1.30.0
// source: user.sql

package sqlc

import (
	"context"

	"github.com/jackc/pgx/v5/pgtype"
)

const createUserStub = `-- name: CreateUserStub :one
INSERT INTO users (id, email, phone_number, role)
VALUES ($1, $2, $3, $4)
RETURNING id, email, phone_number, password_hash, role, created_at
`

type CreateUserStubParams struct {
	ID          pgtype.UUID `json:"id"`
	Email       pgtype.Text `json:"email"`
	PhoneNumber pgtype.Text `json:"phone_number"`
	Role        string      `json:"role"`
}

// Inserts a minimal user row to satisfy patients.user_id FK. Canonical user (with password) lives in auth_service.
func (q *Queries) CreateUserStub(ctx context.Context, arg CreateUserStubParams) (User, error) {
	row := q.db.QueryRow(ctx, createUserStub,
		arg.ID,
		arg.Email,
		arg.PhoneNumber,
		arg.Role,
	)
	var i User
	err := row.Scan(
		&i.ID,
		&i.Email,
		&i.PhoneNumber,
		&i.PasswordHash,
		&i.Role,
		&i.CreatedAt,
	)
	return i, err
}
